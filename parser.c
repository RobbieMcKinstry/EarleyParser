#include <stdlib.h>

int main() {

	// implement earley's algorithm.
	
	// first, open a text file with the Sentence in it.
	// Add every lexeme in the file to the symbol table, and maintain a list of tokens in the sentence.
	// By now you should have an intialized symbol table, a list of tokens, and a grammar.
	// Create Q1 and Q2
	// then initialize a frame with the S nonterminal, and add it to  Q1
	//
	//

	return 0;
}
